# 可视化调试工具设计文档

## 概述

可视化调试工具（Visual Debugger）是一个为 Godot Core System 提供实时可视化调试信息的工具。它通过图形界面展示系统状态、事件流、数据关系等信息，帮助开发者快速理解和调试系统行为。

## 设计目标

1. **实时可视化**: 实时显示系统状态和事件
2. **直观易懂**: 使用图形化界面，降低理解成本
3. **交互式调试**: 支持交互式操作和实时修改
4. **多系统支持**: 支持所有核心系统的可视化
5. **低性能影响**: 调试工具本身不应显著影响性能

## 架构设计

### 核心组件

```
VisualDebugger (主管理器)
├── DebugPanel (调试面板)
│   ├── SystemStatusPanel (系统状态面板)
│   ├── EventFlowPanel (事件流面板)
│   ├── StateMachinePanel (状态机面板)
│   ├── TriggerPanel (触发器面板)
│   ├── TagPanel (标签面板)
│   └── CustomPanel (自定义面板)
├── DebugOverlay (调试覆盖层)
│   ├── SceneOverlay (场景覆盖层)
│   ├── EntityOverlay (实体覆盖层)
│   └── PathOverlay (路径覆盖层)
├── DebugVisualizer (调试可视化器)
│   ├── GraphVisualizer (图形可视化)
│   ├── TimelineVisualizer (时间线可视化)
│   └── TreeVisualizer (树形可视化)
└── DebugController (调试控制器)
    ├── FilterController (过滤器控制器)
    ├── ViewController (视图控制器)
    └── ExportController (导出控制器)
```

## 功能模块

### 1. 系统状态面板 (SystemStatusPanel)

#### 功能
- 显示所有系统的运行状态
- 显示系统资源使用情况
- 显示系统错误和警告
- 支持系统启用/禁用

#### 界面设计
```
┌─────────────────────────────────────┐
│ 系统状态面板                          │
├─────────────────────────────────────┤
│ ✅ 事件系统      [运行中]  [详情]     │
│ ✅ 状态机系统    [运行中]  [详情]     │
│ ⚠️  存档系统      [警告]   [详情]     │
│ ❌ 输入系统      [错误]   [详情]     │
│ ⏸️  音频系统      [暂停]   [详情]     │
├─────────────────────────────────────┤
│ [刷新] [导出] [设置]                │
└─────────────────────────────────────┘
```

#### 实现
```gdscript
class SystemStatusPanel extends Control:
    var system_statuses: Dictionary = {}
    
    func update_system_status(system_name: String, status: Dictionary) -> void:
        system_statuses[system_name] = status
        _refresh_display()
    
    func _refresh_display() -> void:
        # 更新UI显示
        pass
```

### 2. 事件流面板 (EventFlowPanel)

#### 功能
- 实时显示事件流
- 显示事件订阅关系
- 显示事件处理时间
- 支持事件过滤和搜索

#### 界面设计
```
┌─────────────────────────────────────┐
│ 事件流面板                           │
├─────────────────────────────────────┤
│ 过滤器: [全部 ▼] [搜索: _____]      │
├─────────────────────────────────────┤
│ 时间    事件名        订阅者  耗时   │
│ 10:23:45 player_jump  Player  0.5ms │
│ 10:23:46 enemy_spawn EnemyMgr 1.2ms │
│ 10:23:47 level_complete GameMgr 2ms│
├─────────────────────────────────────┤
│ [暂停] [清空] [导出] [设置]          │
└─────────────────────────────────────┘
```

#### 可视化特性
- 事件时间线视图
- 事件订阅关系图
- 事件处理性能热力图
- 事件频率统计

### 3. 状态机面板 (StateMachinePanel)

#### 功能
- 显示所有状态机的状态
- 可视化状态转换图
- 显示状态转换历史
- 支持状态机控制（启动/停止/切换状态）

#### 界面设计
```
┌─────────────────────────────────────┐
│ 状态机面板                           │
├─────────────────────────────────────┤
│ 状态机: [player_fsm ▼]              │
│                                     │
│  ┌─────┐      ┌─────┐              │
│  │ Idle│ ───> │Walk │              │
│  └─────┘      └─────┘              │
│     │            │                  │
│     │            ▼                  │
│     └──────> ┌─────┐              │
│              │Jump │              │
│              └─────┘              │
│                                     │
│ 当前状态: Walk                      │
│ 状态历史: Idle -> Walk -> Jump      │
├─────────────────────────────────────┤
│ [启动] [停止] [切换状态] [导出]     │
└─────────────────────────────────────┘
```

#### 可视化特性
- 状态节点图
- 状态转换动画
- 状态持续时间显示
- 状态转换条件显示

### 4. 触发器面板 (TriggerPanel)

#### 功能
- 显示所有触发器
- 显示触发器状态（激活/未激活）
- 显示触发器条件和动作
- 支持触发器手动触发

#### 界面设计
```
┌─────────────────────────────────────┐
│ 触发器面板                           │
├─────────────────────────────────────┤
│ 触发器列表:                          │
│ ✅ door_open_trigger                │
│   条件: player_in_area = true       │
│   动作: open_door()                 │
│   状态: 已激活                      │
│                                     │
│ ⏸️  enemy_spawn_trigger             │
│   条件: level > 5                   │
│   动作: spawn_enemy()               │
│   状态: 未激活                      │
├─────────────────────────────────────┤
│ [刷新] [触发] [编辑] [导出]          │
└─────────────────────────────────────┘
```

### 5. 标签面板 (TagPanel)

#### 功能
- 显示标签层次结构
- 显示对象标签分配
- 支持标签查询和过滤
- 显示标签使用统计

#### 界面设计
```
┌─────────────────────────────────────┐
│ 标签面板                             │
├─────────────────────────────────────┤
│ 标签树:                              │
│ 📁 Game                              │
│   📁 Character                       │
│     🏷️ Player                        │
│     🏷️ Enemy                         │
│   📁 Item                            │
│     🏷️ Weapon                        │
│     🏷️ Consumable                    │
│                                     │
│ 对象标签:                            │
│ Player (Character.Player)           │
│ Enemy (Character.Enemy)            │
├─────────────────────────────────────┤
│ [查询] [过滤] [统计] [导出]          │
└─────────────────────────────────────┘
```

### 6. 存档系统面板 (SaveSystemPanel)

#### 功能
- 显示存档列表
- 显示存档元数据
- 显示存档大小和格式
- 支持存档操作（加载/删除/导出）

#### 界面设计
```
┌─────────────────────────────────────┐
│ 存档系统面板                         │
├─────────────────────────────────────┤
│ 存档列表:                            │
│ 💾 save_001.json                    │
│    大小: 2.5 MB                     │
│    时间: 2024-01-15 10:23:45        │
│    版本: 1.0.0                      │
│                                     │
│ 💾 auto_save_001.bin                │
│    大小: 1.8 MB                     │
│    时间: 2024-01-15 10:20:00        │
│    版本: 1.0.0                      │
├─────────────────────────────────────┤
│ [新建] [加载] [删除] [导出]          │
└─────────────────────────────────────┘
```

### 7. 输入系统面板 (InputSystemPanel)

#### 功能
- 显示输入映射
- 实时显示输入状态
- 显示输入缓冲
- 支持输入录制和回放

#### 界面设计
```
┌─────────────────────────────────────┐
│ 输入系统面板                         │
├─────────────────────────────────────┤
│ 输入映射:                            │
│ jump: [Space] [W]                   │
│ move_left: [A] [←]                  │
│ move_right: [D] [→]                 │
│                                     │
│ 当前输入状态:                        │
│ move_right: ████████░░ (0.8)        │
│ jump: [按下]                         │
│                                     │
│ 输入缓冲:                           │
│ [jump: 0.1s] [attack: 0.05s]       │
├─────────────────────────────────────┤
│ [录制] [回放] [清除] [设置]          │
└─────────────────────────────────────┘
```

## 调试覆盖层 (DebugOverlay)

### 1. 场景覆盖层 (SceneOverlay)

#### 功能
- 在场景中显示调试信息
- 显示节点层次结构
- 显示节点属性
- 显示碰撞体边界

#### 可视化元素
- 节点边界框
- 碰撞体形状
- 路径和轨迹
- 标签和ID显示

### 2. 实体覆盖层 (EntityOverlay)

#### 功能
- 显示实体位置
- 显示实体组件
- 显示实体关系
- 显示实体状态

### 3. 路径覆盖层 (PathOverlay)

#### 功能
- 显示AI路径
- 显示移动轨迹
- 显示导航网格
- 显示路径点

## 可视化组件

### 1. 图形可视化 (GraphVisualizer)

#### 功能
- 绘制系统关系图
- 绘制状态转换图
- 绘制事件流图
- 支持图形交互（缩放、平移、选择）

#### 实现
```gdscript
class GraphVisualizer extends Control:
    var nodes: Array[GraphNode] = []
    var edges: Array[GraphEdge] = []
    
    func add_node(node: GraphNode) -> void:
        nodes.append(node)
    
    func add_edge(edge: GraphEdge) -> void:
        edges.append(edge)
    
    func _draw() -> void:
        # 绘制节点和边
        pass
```

### 2. 时间线可视化 (TimelineVisualizer)

#### 功能
- 显示事件时间线
- 显示状态转换时间线
- 显示性能数据时间线
- 支持时间范围选择

### 3. 树形可视化 (TreeVisualizer)

#### 功能
- 显示层次结构（标签树、节点树）
- 支持展开/折叠
- 支持搜索和过滤
- 支持拖拽操作

## API 设计

### 主要 API

```gdscript
# 可视化调试器主类
class VisualDebugger extends Control:
    ## 显示调试面板
    func show_panel(panel_type: String) -> void
    
    ## 隐藏调试面板
    func hide_panel(panel_type: String) -> void
    
    ## 切换调试面板显示
    func toggle_panel(panel_type: String) -> void
    
    ## 注册自定义面板
    func register_custom_panel(panel: DebugPanel) -> void
    
    ## 显示调试覆盖层
    func show_overlay(overlay_type: String) -> void
    
    ## 隐藏调试覆盖层
    func hide_overlay(overlay_type: String) -> void
    
    ## 更新系统状态显示
    func update_system_status(system_name: String, status: Dictionary) -> void
    
    ## 记录事件
    func record_event(event_name: String, details: Dictionary) -> void
    
    ## 导出调试数据
    func export_debug_data(format: String) -> void
    
    ## 设置过滤器
    func set_filter(filter_type: String, filter_value: Variant) -> void
```

### 使用示例

```gdscript
# 基本使用
var debugger = CoreSystem.visual_debugger

# 显示系统状态面板
debugger.show_panel("system_status")

# 显示事件流面板
debugger.show_panel("event_flow")

# 显示状态机面板
debugger.show_panel("state_machine")

# 显示场景覆盖层
debugger.show_overlay("scene")

# 记录自定义事件
debugger.record_event("custom_event", {
    "data": "value"
})

# 导出调试数据
debugger.export_debug_data("json")
```

## 配置选项

```gdscript
var debugger_config = {
    "enabled": true,
    "auto_show_on_error": true,
    "panel_update_rate": 10.0,  # 面板更新率（Hz）
    "overlay_enabled": true,
    "overlay_opacity": 0.8,
    "event_history_size": 1000,
    "auto_scroll": true,
    "theme": "dark",  # dark, light
    "font_size": 12,
    "panels": {
        "system_status": true,
        "event_flow": true,
        "state_machine": true,
        "trigger": true,
        "tag": true,
        "save": true,
        "input": true
    }
}
```

## 快捷键支持

| 快捷键 | 功能 |
|--------|------|
| F1 | 显示/隐藏调试面板 |
| F2 | 切换系统状态面板 |
| F3 | 切换事件流面板 |
| F4 | 切换状态机面板 |
| F5 | 切换场景覆盖层 |
| Ctrl+E | 导出调试数据 |
| Ctrl+F | 打开过滤器 |

## 性能优化

### 优化策略
1. **按需更新**: 只更新可见的面板
2. **数据采样**: 降低更新频率
3. **数据限制**: 限制历史数据大小
4. **异步渲染**: 后台处理可视化计算
5. **条件编译**: 发布版本可禁用

### 性能目标
- 调试面板开销 < 2% CPU
- 内存开销 < 20MB
- 对游戏帧率影响 < 2 FPS

## 集成方案

### 1. 作为 CoreSystem 模块

```gdscript
# 在 core_system.gd 中添加
@onready var visual_debugger: VisualDebugger = _get_module("visual_debugger")
```

### 2. 编辑器集成

- 编辑器插件支持
- 调试面板窗口
- 场景视图集成

### 3. 运行时集成

- 游戏内调试面板
- 可切换的调试UI
- 移动设备支持（远程调试）

## 实施计划

### 阶段一：基础框架（2周）
1. 实现调试面板框架
2. 实现系统状态面板
3. 实现事件流面板

### 阶段二：核心功能（2周）
1. 实现状态机面板
2. 实现触发器面板
3. 实现标签面板

### 阶段三：高级功能（2周）
1. 实现调试覆盖层
2. 实现可视化组件
3. 实现自定义面板支持

### 阶段四：优化和完善（1周）
1. 性能优化
2. UI 美化
3. 文档编写

## 未来扩展

1. **远程调试**: 支持通过网络远程调试
2. **录制和回放**: 录制调试会话并回放
3. **AI 辅助**: 使用 AI 分析调试数据
4. **性能分析集成**: 与性能分析工具集成
5. **多语言支持**: 支持多语言界面
6. **自定义主题**: 支持自定义主题和样式

## 总结

可视化调试工具将大大提升开发效率和调试体验。通过直观的图形界面和实时信息显示，开发者可以快速理解系统行为、定位问题并进行调试。
